import {
	Box,
	Container,
	Typography,
	Stack,
	InputBase,
	Button,
	IconButton,
	Icon,
	styled,
} from '@mui/material';

export const IconImage = styled('img')({
	display: 'flex',
	height: 'inherit',
	width: 'inherit',
});
